spring.application.name=event-service

# Current baseline relies on Spring Boot's local defaults.
# The active Qeue service under services/event-service uses explicit environment-based config.

server.port=4000

logging.level.root=info
